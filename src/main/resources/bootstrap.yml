spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: minikube-sample
      reload:
        enabled: true
        mode: event
        strategy: REFRESH