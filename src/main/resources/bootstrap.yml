spring:
  cloud:
    kubernetes:
      config:
        enabled: false
        sources:
          - namespace: default
            name: minikube-sample
      reload:
        enabled: true
        mode: event
        strategy: polling